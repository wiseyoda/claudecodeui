# Restart Backend and Frontend

Run the restart.sh script to kill existing backend (port 3002) and frontend (port 5173) processes, then start them fresh in development mode.

Execute: `./restart.sh`

This command has permission to run bash commands.
